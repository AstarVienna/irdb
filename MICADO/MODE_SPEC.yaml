# yaml extension for the wide field imaging mode
properties :
    pixel_scale : 0.004

effects :
-   name: wide_mode_optics
    description: list of extra mirror needed for the wide field mode
    class: SurfaceList
    kwargs:
        filename: LIST_mirrors_MICADO_Wide.tbl
